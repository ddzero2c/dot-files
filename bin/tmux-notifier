#!/bin/bash
msg="${1:-Done}"
gid="${2:-default}"
tmux=/opt/homebrew/bin/tmux

if [ -n "$TMUX" ]; then
  session=$(tmux display-message -t "$TMUX_PANE" -p '#S')
  client=$(tmux display-message -t "$TMUX_PANE" -p '#{client_tty}')

  terminal-notifier \
    -title "TMUX($session)" \
    -message "$msg" \
    -group "claude-$gid" \
    -contentImage ~/assets/claude.svg \
    -activate com.mitchellh.ghostty \
    -execute "$tmux switch-client -c $client -t $TMUX_PANE"
else
  terminal-notifier \
    -title "Terminal" \
    -message "$msg" \
    -group "claude-$gid" \
    -contentImage ~/assets/claude.svg \
    -activate com.mitchellh.ghostty
fi
